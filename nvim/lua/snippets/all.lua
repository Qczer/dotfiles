local ls = require 'luasnip'
local s = ls.snippet
local t = ls.text_node

return {
  s('name', t('Mateusz Kuczera')),
  s('nick', t('Qczer')),
  s('mail', t('mkuczera@proton.me')),
  s('email', t('mkuczera@proton.me')),
  s('gmail', t('mateuszkuczera3@gmail.com')),
  s('gh', t('github.com/Qczer')),
  s('portfolio', t('qczer.github.io')),
}
